name: Android CI

on:
  pull_request:
    branches: [ develop ]

jobs:
  validate-change:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build
      run: ./gradlew --no-daemon --max-workers 8 --build-cache assemble
    - name: Validating Ktlint
      run: ./gradlew --no-daemon --max-workers 8 --build-cache ktlintDebugCheck
    - name: Verifying detekt
      run: ./gradlew --no-daemon --max-workers 8 --build-cache detekt

